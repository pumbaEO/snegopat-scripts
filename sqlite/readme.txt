ѕока умеет немного - открыть базу, выполнить произвольный запрос, вернуть результат.

ћетоды:

OpenDatabase(dbFile) - открыть базу по пути. ѕри этом создаетс€ внутренний объект дл€ работы с базой, собственно открыти€ файла не происходит.
¬озвращает строку с идентификатором базы, его нужно использовать дальше дл€ работы с базой.

CloseDatabase(db) - освободить внутренний объект дл€ указанной базы.
¬озвращает булево - true или false. False может быть, если переданный идентификатор не существует.

ExecSQL(db, sql) - выполнить произвольный запрос в указанной базе.
¬озвращает:
- дл€ запросов вида SELECT - таблицу значений (ValueTable)
- дл€ остальных запросов - количество "задетых" строк (Affected rows)
- при возникновении ошибки - False
“акже при возникновении ошибки ругаетс€ в окно сообщений с описанием что именно не так.
